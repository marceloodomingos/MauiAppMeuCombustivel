namespace MauiAppMeuCombustivel // Define o namespace da aplicação, organizando as classes.
{
    public partial class AppShell : Shell // Define a classe AppShell, que herda de Shell, representando a estrutura da aplicação.
    {
        public AppShell() // Construtor da classe AppShell.
        {
            InitializeComponent(); // Inicializa os componentes definidos no XAML para esta shell.
        }
    } // Fecha a classe AppShell
} // Fecha o namespace